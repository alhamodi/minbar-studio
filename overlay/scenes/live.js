import BaseScene from './BaseScene.js';

export default class extends BaseScene {
    
    getHTML(state) {
        return `<div class="scene-layout transparent">
            <!-- Widgets handle the UI here -->
        </div>`;
    }
    update(state) {}
    
    
}
